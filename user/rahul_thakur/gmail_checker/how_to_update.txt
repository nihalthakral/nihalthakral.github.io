1. Extension ka code update karo

2. manifest.json mein version badhao:

"version": "1.1"
  
3. Chrome se re-pack karo same .pem se → naya .crx banega
  
4. GitHub par yeh 2 cheezein upload karo:

Naya .crx → same URL par (replace)
updates.xml mein sirf version change karo:

xmlversion='1.1'
  
Bas. Chrome khud ~5 ghante mein check karta hai aur silently update kar deta hai. Client ko kuch pata nahi chalega.
